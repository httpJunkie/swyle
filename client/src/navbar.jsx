import React from 'react';

const NavBar = props => {
    return (
        <div className="nav-bar">
            <span>This is a navbar, will eventually have links in it</span>
        </div>
    )
}

export default NavBar;